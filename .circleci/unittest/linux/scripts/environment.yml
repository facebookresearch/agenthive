channels:
  - pytorch
  - defaults
dependencies:
  - pip
  - pip:
    - dm_control
    - hypothesis
    - future
    - cloudpickle
    - pytest
    - pytest-cov
    - pytest-mock
    - pytest-instafail
    - expecttest
    - pyyaml
    - scipy
    - coverage
