# @package _global_
hydra:
  run:
    dir: /scratch/cluster/rutavms/robohive/outputs_sac_robohive/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.num}_${hydra.job.override_dirname}
  sweep:
    dir: /scratch/cluster/rutavms/robohive/outputs_sac_robohive/${hydra.job.name}/${now:%Y-%m-%d}_${now:%H-%M-%S}
    subdir: ${hydra.job.num}_${hydra.job.override_dirname}
